{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "type-enum": [2, "always", ["feat", "fix", "docs", "refactor", "test", "chore"]],
    "scope-enum": [2, "always", ["backend", "admin-web", "c-web", "miniapp-client", "libs", "deploy"]]
  }
}
