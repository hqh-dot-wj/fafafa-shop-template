---
description: 安全与稳定性（幂等、限流、敏感数据、超时、错误隐藏）
alwaysApply: false
---

# 安全与稳定性

## 幂等与防重（P0）

- 支付/下单/创建类接口必须幂等：唯一约束、Redis SetNX、分布式锁、状态机
- 防重复提交：Redis 记录 `userId+业务key`，TTL 3–5 秒

## 敏感数据（P0）

- 日志和接口返回禁止明文：手机号、身份证、银行卡、密码；脱敏示例 `138****1234`

## 限流（P1）

- 高 QPS 接口必须限流；维度：IP、用户、接口；实现：令牌桶、Nginx、Gateway、Redis

## 超时与弱依赖（P1）

- RPC/HTTP 必须设超时（3–5s）；非核心依赖做弱依赖：超时/失败不影响主流程

## 错误信息（P1）

- 生产环境不向前端返回堆栈、SQL、内部路径；统一错误码 + 通用文案，详情写日志
