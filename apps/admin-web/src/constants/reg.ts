/**
 * 正则校验常量
 * 统一从 @libs/common-constants 导出，保持向后兼容
 */
export {
  REG_USER_NAME,
  REG_PHONE,
  REG_PWD,
  REG_EMAIL,
  REG_CODE_SIX,
  REG_CODE_FOUR,
  REG_URL,
} from '@libs/common-constants/regex';
